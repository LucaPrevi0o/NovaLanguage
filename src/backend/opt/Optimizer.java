package backend.opt;

import backend.ir.IRProgram;

/// Optimization scaffold (constant folding / dead-code elimination hooks).
public class Optimizer {

    public IRProgram optimize(IRProgram program) {
        // Placeholder pass manager; currently a no-op.
        return program;
    }
}
