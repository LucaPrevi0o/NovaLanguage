/**
 * Shared AST node types used by Nova statements, expressions, symbols, and type syntax.
 *
 * <p>This package contains common node-level abstractions that are reused by more specific
 * AST subpackages. These classes form the bridge between parser output and later semantic
 * analysis.</p>
 */
package parser.ast.nodes;
