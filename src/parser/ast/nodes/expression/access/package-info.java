/**
 * AST nodes for expression forms that access nested or indexed values.
 *
 * <p>This package contains expression nodes for constructs such as member access and array
 * indexing. These nodes describe the syntactic shape of access expressions; semantic passes
 * are responsible for verifying that the accessed member, field, method, or array element
 * actually exists and is valid.</p>
 */
package parser.ast.nodes.expression.access;
