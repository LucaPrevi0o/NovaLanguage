/**
 * AST nodes for numeric literal expressions.
 *
 * <p>This package contains concrete expression nodes for Nova numeric literal forms, such as
 * byte, integer, long, float, and double literals. These nodes preserve the parsed literal
 * category so later semantic analysis can infer or validate the expression type.</p>
 */
package parser.ast.nodes.expression.literal.number;
