/**
 * Concrete token wrappers for specific Nova token categories.
 *
 * <p>Classes in this package wrap token-family information into token objects that can be
 * passed through the compiler front-end pipeline. They provide category-specific token
 * representations while preserving the common token contract expected by the parser and
 * diagnostics.</p>
 */
package lexer.token.type;
